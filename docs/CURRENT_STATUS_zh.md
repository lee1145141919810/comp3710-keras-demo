# 当前状态（2026-09-18）

此文件更新旧 Medium 文档中的状态；旧文档记录历史核对过程，不能作为当前进度表。原始评分 PDF 已核对，详见 HARD_RUBRIC_VERIFIED_zh.md。Hard上限7/7；当前不能认定所有评分项已满足。

| 项目 | 当前可核实状态 |
|---|---|
| OASIS | 原始 ZIP 哈希与此前课程包记录一致，22,656 PNG 解压校验通过 |
| Part 1 | 本机 RTX 5080 完成 N=256 至8192的6种规模计时；不是 Rangpur 实测 |
| VAE | 已恢复训练模型，本机 GPU 重新生成重建图、先验采样、流形及插值 |
| UNet | 本机 GPU 完整544张测试图复核；DSC：0.99820/0.92671/0.94007/0.96651；纯推理约0.302秒 |
| GAN | 已训练改进版60轮，独立样本多样性明显改善；已重新加载推理与插值 |
| DAWNBench | 分享对话记录学校A100：TTA 94.00%，无TTA 93.55%，纯训练91.3秒，含评价首次达标约99.4秒；本机尚无原始模型/JSON，不能当作本次复测 |
| Part 2/3.1 | 仍为历史结果，本次未重训 |
| Git课程 | 尚无完成证明 |
| Rangpur演示 | 新的一键脚本已做本机DFT/VAE/UNet/GAN实跑；集群CIFAR部分尚未实跑 |
| Hard评分 | 原始PDF已核对；GAN真实性由老师判断，仍不保证分数 |

## 这次新增

scripts/collect_demo_evidence.py：输出目录必须是新目录；记录主机、GPU、软件版本、Slurm ID、权重SHA-256、每阶段命令/退出码/耗时及日志。Windows检查不会标成Rangpur结果。--cluster要求Slurm作业并运行CIFAR测试推理（开启TTA）及从已训练权重开始的一轮训练，输出至新目录。

slurm/full_demo.slurm：后台演示作业模板，默认a100来自此前成功的分区记录，提交前应查看当前sinfo。省略显式内存请求以避免此前出现的配置不接受问题；若集群再次拒绝，按实际报错调整。

DFT和VAE可视化增加--output_dir，避免覆盖原结果。GAN当前支持载入模型生成新样本，训练脚本尚未提供--resume。

演示顺序：解释DFT计时→VAE潜空间→UNet四类DSC→GAN原始/改进对比→CIFAR推理和单轮训练。脚本通过不代表本人答辩已完成。

GitHub尚未推送本次Windows修改；交付包保留完整代码和模型。
